{
  "name": "betachat",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "vapid": "node scripts/generate-vapid-keys.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
